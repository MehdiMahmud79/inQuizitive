// import React, { useState, useEffect } from "react";
// import ResultList from './QuizSearchForm';

// import API from '../utils/API';

// const QuizSearch = () => {
//     const [result, setResult] = useState({});
//     const [search, setSearch] = useState('');
  
//     // When the search form is submitted, use the API.search method to search for the movie(s)
//     const searchQuiz = (query) =>
//       API.search(query)
//         .then((res) => setResult(res.data))
//         .catch((err) => console.log(err));
//   return (
//     <div>
//       {/* Pass our results to the ResultsList component to map over */}
//       <ResultList results={results} />
//     </div>
//   );
  
//   };

// export default QuizSearch;
